<section class="review-card">
  <a href="/review/{{id}}">
    <header>Title: {{title}}</header>
    <p>Description: {{description}}</p>
    <p>Guest: {{taster_name}} </p>
    <img src="/images/{{reviews.[0].filename}}" alt="{{reviews.[0].description}}">
  </a>
</section>